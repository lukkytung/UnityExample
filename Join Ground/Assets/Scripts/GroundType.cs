
// 地板类型
public enum GroundType
{
    
    // 直行
    Straight = 0,
    // 水平直行：从左往右
    LeftRight = 1,
    // 水平直行：从右往左
    RightLeft = 2,
    // 从底部进入右转
    BottomRight = 3,
    // 从底部进入左转
    BottomLeft = 4,
    // 从右进入向上
    RightTop = 5,
    // 从左进入向上
    LeftTop = 6,
}